 # Пример файла окружения для фронтенда
# Скопируйте в .env.local (или .env) и отредактируйте значения

# Базовый URL API (у NestJS глобальный префикс /api/afisha)
VITE_API_URL=/api/afisha

# Базовый URL CDN для изображений (отдаётся бэкендом как статика)
VITE_CDN_URL=/content/afisha

# Клавиша для открытия формы входа администратора (KeyboardEvent.code)
REACT_APP_ADMIN_TRIGGER_KEY=KeyA

#  Пароль администратора для локальной разработки (если используется во фронте)
# REACT_APP_ADMIN_SECRET=admin
